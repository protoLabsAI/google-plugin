[pytest]
# Lives in tests/ so rootdir is tests/, not the plugin package root — pytest then
# never imports the plugin's __init__.py (whose relative imports resolve only at
# runtime). importlib mode imports test files by path. Tests load the service
# modules by file path; see test_google.py.
addopts = --import-mode=importlib
